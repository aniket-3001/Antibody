# Antibody API — Python 3.11+
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart

# Memory — the star. Pinned to the version verified locally.
cognee[fastembed]==1.2.2

# Multimodal intake (spec §11). Voice→transcript for scam-call clips is enabled
# by default; intake still degrades gracefully if these are absent.
faster-whisper            # scam-call / voicemail audio transcription (CPU, no key)
# Optional, needs the Tesseract binary on PATH:
#   pip install pytesseract pillow      # SMS screenshot OCR
