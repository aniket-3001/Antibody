# MemoryOS Backend dependencies
# Install into the same environment as memory_core.
# From the repo root:
#   pip install -r Backend/requirements.txt
#
# memory_core dependencies (cognee, anthropic, fastembed) must be installed
# separately — they live in the prototype/.venv on the developer machine.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0          # for test_client in integration tests
pymupdf>=1.28.0        # for memory_core PDF ingestion adapter
